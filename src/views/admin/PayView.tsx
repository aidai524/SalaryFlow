import { PlaceholderView } from "../PlaceholderView";

export function PayView() {
  return (
    <PlaceholderView
      title="Pay"
      description="Admin home (Pay). Implement from Figma node Decash-Pay; keep the AppHeader shell and replace this placeholder content."
    />
  );
}
